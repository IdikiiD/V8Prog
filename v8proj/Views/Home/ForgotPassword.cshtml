<div class="min-h-screen flex items-center justify-center bg-gray-100 py-12 px-4 sm:px-6 lg:px-8">
    <div class="max-w-md w-full bg-white rounded-lg shadow-lg p-8">
        <!-- Заголовок -->
        <div class="text-center mb-8">
            <h2 class="text-3xl font-bold text-gray-900">Восстановление пароля</h2>
            <p class="mt-2 text-gray-600">
                Введите email, указанный при регистрации, и мы отправим вам инструкции по восстановлению пароля
            </p>
        </div>

        <!-- Форма -->
        <form class="space-y-6" action="@Url.Action("ForgotPassword", "Home")" method="POST">
            <!-- Email поле -->
            <div>
                <label for="email" class="block text-sm font-medium text-gray-700">
                    Email
                </label>
                <div class="mt-1">
                    <input id="email"
                           name="email"
                           type="email"
                           required
                           class="appearance-none block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm placeholder-gray-400 focus:outline-none focus:ring-blue-500 focus:border-blue-500">
                </div>
            </div>

            <!-- Кнопка отправки -->
            <div>
                <button type="submit"
                        class="w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-black hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition duration-150 ease-in-out">
                    Отправить инструкции
                </button>
            </div>

            <!-- Дополнительные ссылки -->
            <div class="flex items-center justify-between mt-4">
                <div class="text-sm">
                    <a href="@Url.Action("Avtorization", "Home")" class="font-medium text-black hover:text-blue-500">
                        Вернуться к входу
                    </a>
                </div>
                <div class="text-sm">
                    <a href="@Url.Action("Registration", "Home")" class="font-medium text-black hover:text-blue-500">
                        Создать аккаунт
                    </a>
                </div>
            </div>
        </form>

        <!-- Дополнительная информация -->
        <div class="mt-6 border-t border-gray-200 pt-6">
            <div class="text-sm text-gray-600">
                <p class="mb-2">Если вы не получили письмо:</p>
                <ul class="list-disc pl-5 space-y-1">
                    <li>Проверьте папку "Спам"</li>
                    <li>Убедитесь, что указали правильный email</li>
                    <li>Подождите несколько минут и попробуйте снова</li>
                </ul>
            </div>
        </div>
    </div>
</div>